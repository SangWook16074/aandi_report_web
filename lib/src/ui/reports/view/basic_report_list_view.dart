import 'package:flutter/material.dart';

class BasicReportListView extends StatelessWidget {
  const BasicReportListView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        // ReportListWidget(label: "1주차 과제"),
        // ReportListWidget(label: "2주차 과제"),
      ],
    );
  }
}
